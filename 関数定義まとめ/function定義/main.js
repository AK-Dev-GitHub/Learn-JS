//関数の宣言を行うにはfunction命令を使用します。
// 関数名には処理の説明的な名前をつけるのが一般的です。
function greet() {
  console.log('こんにちは!');
}

greet();

// オリジナル練習関数
let fruitName =
  [`apple`, `banana`, `orange`] + 'が買い物かごに入っている果物です。';
function fruitNameOutput() {
  console.log(fruitName);
}

fruitNameOutput();
